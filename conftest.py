"""Defines the project-wide fixtures.

This file contains fixtures that are used across the entire project. Fixtures are a way of providing a fixed baseline upon which tests can reliably and repeatedly execute.

Example:

@pytest.fixture
def example_fixture():
    return 'example'
"""
